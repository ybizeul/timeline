apiVersion: v1
kind: ConfigMap
metadata:
  name: timeline-server-config
  namespace: timeline
data:
  HTTP_ADDR: ":8080"
  MONGO_URI: "mongodb://timeline-mongodb:27017"
  MONGO_DATABASE: "timeline"
  PUBLIC_BASE_URL: "https://timeline.example.com"
  COOKIE_SECURE: "true"
  COOKIE_SAMESITE: "Lax"
