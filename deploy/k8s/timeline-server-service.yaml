apiVersion: v1
kind: Service
metadata:
  name: timeline-server
  namespace: timeline
spec:
  selector:
    app: timeline-server
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
